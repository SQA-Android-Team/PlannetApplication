<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.sqa.plannet">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.PlannetApplication">
        <activity android:name=".MainActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name=".activity.subject.SubjectCreateActivity" />
        <activity android:name=".activity.subject.SubjectDetailActivity"/>
        <activity android:name=".activity.subject.SubjectEditActivity"/>
        <activity android:name=".activity.subject.SubjectViewActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name=".activity.teacher.TeacherCreateActivity"/>
        <activity android:name=".activity.teacher.TeacherDetailActivity"/>
        <activity android:name=".activity.teacher.TeacherEditActivity"/>
        <activity android:name=".activity.teacher.TeacherViewActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>



    </application>

</manifest>